           <div id="main-content" class="main-content container-fluid">
            <!-- // page head -->

            <div id="page-content" class="page-content">
                <section>
                    <div class="row-fluid margin-top20">
                        <div class="span12">
                            <div class="widget widget-box widget-table">
                                <div class="widget-header">
                                    <h4><i class="fontello-icon-users"></i> Listagem</h4>
                                </div>
                                <table class="table table-striped table-condensed table-content boo-table">
                                  <tr>
                                    <th></th>
                                    <th>Número</th>
                                    <th>Endereço</th>
                                    <th>Bairro</th>
                                    <th>Cidade</th>
                                    <th>Valor</th>
                                    <th>Sentido</th>
                                    <th>Fotos</th>
                                    <th></th>
                                </tr>

                                <% @outdoors.each { |outdoor| %>
                                <tr>
                                    <td><%= check_box_tag("delete[#{outdoor.id}]",1) %></td>
                                    <td><%= link_to outdoor.numero, [:admin, outdoor] %></td>
                                    <td><%= outdoor.endereco %></td>
                                    <td><%= outdoor.bairro %></td>
                                    <td><%= outdoor.cidade %></td>
                                    <td><%= humanized_money_with_symbol outdoor.valor %></td>
                                    <td><%= outdoor.sentido %></td>
                                    <td>
                                        <% outdoor.foto_outdoors.each { |foto|%>
                                            <%= image_tag foto.file.url(:small) %>
                                        <% } %>
                                    </td>
                                    <td>
                                        <%= link_to 'Edit', edit_admin_outdoor_path(outdoor), class: "btn btn-blue btn-mini" %>
                                        <%= link_to 'Destroy', [:admin, outdoor], method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-yellow btn-mini" %></td>
                                    </tr>
                                    <% } %>
                                </table>
                                <div class="widget-footer">
                                    <div class="btn-toolbar pull-left">
                                        <%= link_to 'Novo Outdoor', new_admin_outdoor_path, class: "btn btn-blue" %>
                                    </div>
                                </div>

                            </div>                           
                        </div>
                    </div>
                </section>
            </div>

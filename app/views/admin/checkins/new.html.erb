
                            <div class="widget widget-simple">
                                <div class="widget-header">
                                    <h4><i class="fontello-icon-check"></i> Novo check-in
                                    	<br/>
                                    	<small><%="#{@checkin.reserva.cliente.nome}"%></br><%="#{@checkin.reserva.inicio_reserva.strftime("%d/%m/%Y")} - #{@checkin.reserva.termino_reserva.strftime("%d/%m/%Y")}"%></small>
									</h4>
                                </div>
                                <div class="widget-content">
									<%= render 'form' %>

								</div>
							</div>

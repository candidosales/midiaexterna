
                            <div class="widget widget-box widget-table">
                                <div class="widget-header">
                                    <h4><i class="fontello-icon-users"></i> Listagem</h4>
                                </div>
                                <table class="table table-striped table-condensed table-content boo-table">
                                    <thead>
                                      <tr>
                                        <th scope="col">Nome</th>
                                        <th scope="col">Email</th>
                                        <th scope="col"></th>
                                    </tr>
                                </thead>

                                <% @usuarios.each do |usuario| %>
                                <tr>
                                    <td><%= link_to usuario.name, [usuario] %></td>
                                    <td><%= usuario.email %></td>
                                    <td>
                                    <%= link_to 'Editar', edit_usuario_registration_path, class: "btn btn-blue btn-mini" %>
                                    <%= link_to 'Apagar', [usuario], method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-yellow btn-mini" %></td>
                                </tr>
                                <% end %>
                            </table>
                            <div class="widget-footer">
                                <div class="btn-toolbar pull-left">
                                    <%= link_to 'Novo Usuário', new_usuario_path, class: "btn btn-blue" %>
                                </div>
                            </div>

                        </div>                           

